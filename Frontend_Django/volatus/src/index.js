import React from 'react';
import ReactDOM from 'react-dom/client';

import RoutesVolatus from './Routes';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RoutesVolatus />
  </React.StrictMode>
);
